# 배경음악(BGM) 폴더

이 폴더에 배경음악 파일을 넣으세요.

- 기본 경로: `/bgm/wedding-bgm.mp3` (즉, 이 폴더에 `wedding-bgm.mp3` 로 저장)
- 경로/파일명을 바꾸려면 `src/data/invitation.js` 의 `bgm.src` 값을 수정하세요.
- 음악을 사용하지 않으려면 `bgm.src` 를 `null` 로 설정하면 우상단 음악 버튼이 숨겨집니다.

⚠️ 저작권에 유의하여 사용 권한이 있는 음원만 사용하세요.
